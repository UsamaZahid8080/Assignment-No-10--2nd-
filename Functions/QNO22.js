//Write a js program to display all array elements using function.
function displayArrayElements(arr) {
    for (let i = 0; i < arr.length; i++) {
      console.log(arr[i]);
    }
  }
  
  const myArray = [1, 2, 3, 4, 5];
  displayArrayElements(myArray);
  